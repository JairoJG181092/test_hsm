<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.coltomex</groupId>
    <artifactId>test_hsm</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>test_hsm</name>
    <description>HSM encryption service for individual and bulk file processing</description>

    <properties>
        <java.version>21</java.version>
        <nimbus-jose-jwt.version>10.9.1</n        <bouncycastle.version>1.85.2</bouncycastle.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
            <version>${nimbus-jose-jwt.version}</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>

        <!-- Realsec proprietary PKCS#11 provider/wrapper. Keep these artifacts in the
             organization's Maven repository or local repository as required by the HSM vendor. -->
        <dependency>
            <groupId>es.provider</groupId>
            <artifactId>RealsecPKCS11Provider</artifactId>
            <version>0.0.17.891</version>
        </dependency>
        <dependency>
            <groupId>realsec.pkcs.pkcs11</groupId>
            <artifactId>realsecPkcs11Wrapper</artifactId>
            <version>1.3.0.629</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
